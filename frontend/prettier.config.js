/** @type {import('prettier').Config} */
const prettierConfig = {
  semi: true,
  trailingComma: 'all',
  singleQuote: true,
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  plugins: [],
};

module.exports = prettierConfig;
